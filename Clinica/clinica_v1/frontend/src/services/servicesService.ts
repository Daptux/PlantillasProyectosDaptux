import { api } from "./api";

// TODO (proxima entrega): metodos del modulo services.
// Usar el cliente axios centralizado `api` que ya inyecta el JWT.
export const servicesService = {
  // ejemplo: list: async () => (await api.get("/services")).data.data,
};

export { api };

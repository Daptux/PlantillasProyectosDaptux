import { api } from "./api";

// TODO (proxima entrega): metodos del modulo patients.
// Usar el cliente axios centralizado `api` que ya inyecta el JWT.
export const patientsService = {
  // ejemplo: list: async () => (await api.get("/patients")).data.data,
};

export { api };

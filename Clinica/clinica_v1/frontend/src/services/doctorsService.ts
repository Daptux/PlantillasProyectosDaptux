import { api } from "./api";

// TODO (proxima entrega): metodos del modulo doctors.
// Usar el cliente axios centralizado `api` que ya inyecta el JWT.
export const doctorsService = {
  // ejemplo: list: async () => (await api.get("/doctors")).data.data,
};

export { api };
